# Получите полный путь к файлу, который содержит
# IP-адрес используемого вами DNS-сервера

# resolver представляет собой набор подпрограмм,
# которые обеспечивают доступ к системе доменных имен в Интернете (DNS).
# Файл resolv.conf содержит информацию, которая читается resolver'ом.

# /etc/resolv.conf
