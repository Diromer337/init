# Определите IP-адрес DNS, который отвечает на следующий URL: slash16.org.

# nslookup - утилита, предоставляющая пользователю интерфейс
# командной строки для обращения к системе DNS.
# Первая строка команды nslookup содержить ip-адрес ответившего DNS

nslookup slash16.org | grep 'Server' | awk '{print $2}'
