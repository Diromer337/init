# Определите шлюз по умолчанию в таблице маршрутизации

# netstat - отображает статус сети
# -r показывает таблицы маршрутизации.
# -n показывает сетевые адреса как числа

netstat -nr | grep default | awk '{print $2}'
