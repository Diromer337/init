# Получите список сетевых интерфейсов, не отображая
# никаких деталей для этих интерфейсов. Только список имен.

# ifconfig без параметров - состяние активных интерфейсов
# -l Вывести список всех доступных интерфейсов в системе без дополнительной информации.

ifconfig -l
