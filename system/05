# Какая команда определяет состояние SSH?

# SSH - это специальный сетевой протокол, позволяющий получать удаленный доступ
# к компьютеру с большой степенью безопасности соединения.

# Установка SSH
# apt-get install openssh-server

service ssh status